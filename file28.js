// const names = ["john", "cathy", "amy"];

// names.forEach((name, index,arr) => {//first argument will always print array value,second argyument print index number
//     console.log(index, name);
// });


const names = ["john", "cathy", "amy"];
const score=[90,20,30]

names.forEach((name, index) => {
    console.log(name,score[index]);
});
